<!-- navbar boostrap -->
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-md">
        <h1 class="navbar-brand" >MyWeb.</h1>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link 
              {{#if (equal currentPage 'Home') }}
                active fw-semibold
              {{/if}}
              " href="/index">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link
                {{#if (equal currentPage 'Project') }}
                active fw-semibold
              {{/if}}
              " href="/projectList">My Project</a>
            </li>
            <li class="nav-item">
              <a class="nav-link
                {{#if (equal currentPage 'Blog') }}
                active fw-semibold
              {{/if}}
              " href="/blog-list">Blog</a>
            </li>
          </ul>
            <a href="/contact" class="btn btn-outline-primary" type="submit">Contact</a>
        </div>
      </div>
    </nav>